zuul:
  retryable: true
  routes:
    shipstation-api:
      path: /esb/shipstation/rates
      serviceId: shipstation-api
ribbon:
  eureka:
    enabled: false
shipstation-api:
  ribbon:
    NIWSServerListClassName: com.netflix.loadbalancer.ConfigurationBasedServerList
    listOfServers: http://localhost:20080
turbine:
  app-config: shipstation-api
hystrix:
  command:
    default:
      circuitBreaker:
        enabled: false
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 210000
management:
  security:
    enabled: false
